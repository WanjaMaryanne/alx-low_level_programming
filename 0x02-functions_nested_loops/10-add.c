#include "main.h"
/**
* add -  adds two integers and returns the result.
*@x:first number  being added.
*@y:second number being added.
* Return: Always 0.
*/
int add(int x, int y)
{
	return (x + y);
}
